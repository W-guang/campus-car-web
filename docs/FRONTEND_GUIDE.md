# 前端文件解析指南

> 本文档面向开发团队，说明项目前端结构、各文件用途及修改指南

---

## 📁 项目结构

```
campus-car-web/
├── frontend/              # 前端页面文件夹
│   ├── index.html        # ✅ 首页 - 地图展示
│   ├── login.html        # ✅ 登录页
│   ├── register.html     # ✅ 注册页
│   ├── forum.html        # ✅ 论坛页 - 核心功能
│   ├── carRent.html      # ✅ 租车页
│   ├── personalInfo.html # ✅ 个人信息页
│   ├── rent.html         # ✅ 车辆发布页
│   ├── admin.html        # ✅ 管理员后台
│   ├── takeService.html  # ⚠️ 重定向页（可选）
│   └── post.html         # ❌ 废弃文件（可删除）
├── assets/
│   └── style.css         # 全局样式
└── backend/
    └── server.js         # 后端API服务
```

**图例说明**：
- ✅ 正常使用的页面
- ⚠️ 重定向页面，向后兼容（可选保留）
- ❌ 废弃文件，建议删除

---

## 🎨 技术栈

- **前端框架**：原生HTML + JavaScript (ES6+)
- **UI框架**：Bootstrap 5.3.0
- **地图服务**：高德地图API 2.0
- **图标库**：Bootstrap Icons 1.10.5
- **HTTP请求**：Fetch API

---

## 📄 页面详解

### 1. login.html - 登录页

**功能**：用户登录验证

**核心元素**：
- 学号输入框 `#studentId`
- 密码输入框 `#password`
- 登录按钮 `#loginBtn`

**关键代码位置**：
- 登录逻辑：第50-80行
- API调用：`POST /api/login`
- 成功后跳转：`window.location.href = "/index.html"`

**样式**：
- 居中布局 `.auth-container`
- 卡片样式 `.auth-card`

**修改建议**：
- 如需修改UI：调整第10-30行的CSS
- 如需添加验证码：在第40行后添加验证码组件

---

### 2. register.html - 注册页

**功能**：新用户注册

**核心元素**：
- 学号、姓名、手机号、密码输入框
- 注册按钮 `#registerBtn`

**关键代码**：
- 表单验证：第60-75行
- API调用：`POST /api/register`
- 手机号正则：`/^1[3-9]\d{9}$/`

**修改建议**：
- 增加密码强度提示：在第50行添加实时验证
- 添加学号格式验证：修改第65行

---

### 3. index.html - 首页

**功能**：地图展示可用车辆

**核心组件**：
- 高德地图容器 `#map-container`
- 统计卡片区 `.stat-card`
- 车辆列表 `#vehicle-list`

**关键功能**：
- 地图初始化：第150-180行
- 加载车辆标记：第200-250行
- 点击标记显示详情：第260-280行

**API接口**：
- `GET /api/vehicles` - 获取车辆列表
- `GET /api/stats` - 获取统计数据

**修改建议**：
- 更换地图主题：修改第155行 `mapStyle`
- 调整统计卡片：修改第100-130行HTML结构
- 修改地图高度：调整第21-26行CSS

---

### 4. forum.html - 论坛页 ⭐核心

**功能**：发布/浏览/管理互助帖子

**页面结构**：
```
导航栏
├── 帖子分类标签 (代拿快递/外卖/求合租车/经验分享)
├── 帖子列表展示区
├── 发布帖子按钮
├── "我的帖子"管理区
│   ├── 我发布的
│   ├── 我接单的
│   └── 其他帖子
└── Modal弹窗组件
```

**核心元素**：
- 帖子列表容器：`#postList` (第280行)
- 发布按钮：`#publishPostBtn` (第268行)
- 发布Modal：`#publishModal` (第300-485行)
- 我的帖子区：`#takeServiceSection` (第287-301行)
- 接单详情Modal：`#acceptDetailModal` (第486-529行)

**关键功能代码位置**：

| 功能 | 代码行数 | 说明 |
|-----|---------|------|
| 标签切换 | 615-630 | 筛选不同类型帖子 |
| 加载帖子列表 | 650-720 | 从API获取并渲染 |
| 发布帖子 | 730-850 | 表单验证+提交 |
| 时间验证 | 762-808 | 不允许过去时间 |
| 接单功能 | 690-724 | 用户接单逻辑 |
| 我发布的帖子 | 1000-1145 | 管理自己的帖子 |
| 我接单的帖子 | 1147-1238 | 查看接单记录 |
| 其他帖子(分享) | 1240-1351 | 求合租车/经验分享 |
| 删除帖子 | 1303-1346 | 删除自己的帖子 |

**API接口**：
```javascript
GET  /api/posts?type=express         // 获取代拿快递帖子
GET  /api/posts?type=takeaway        // 获取代拿外卖帖子
GET  /api/posts?type=carShare        // 获取求合租车帖子
GET  /api/posts?type=experience      // 获取经验分享帖子
POST /api/posts                      // 发布新帖子
POST /api/posts/accept               // 接单
DELETE /api/posts/:post_id           // 删除帖子
GET  /api/posts/my?type=published    // 我发布的
GET  /api/posts/my?type=accepted     // 我接单的
```

**样式定制**：
- 标签样式：第80-120行CSS
- 帖子卡片：第150-200行CSS
- Modal样式：Bootstrap默认+自定义覆盖

**修改建议**：
1. **修改帖子卡片样式**：调整第150-200行CSS
2. **添加新的帖子类型**：
   - 在第300行添加新的表单字段
   - 在第730行添加验证逻辑
   - 在后端server.js添加对应处理
3. **修改时间验证规则**：修改第762、784、806行的验证逻辑
4. **自定义Modal样式**：修改第300-529行的Modal HTML

---

### 5. personalInfo.html - 个人信息页

**功能**：查看/编辑个人资料、修改密码

**核心组件**：
- 个人信息展示表单：第190-215行
- 编辑信息Modal：第225-266行
- 修改密码Modal：第268-299行
- 角色功能区：第217-221行

**关键功能**：

| 功能 | 代码行数 | 说明 |
|-----|---------|------|
| 加载个人信息 | 401-421 | 填充表单数据 |
| 编辑信息 | 489-554 | 修改姓名/手机号 |
| 修改密码 | 556-629 | 验证并更新密码 |
| 角色功能区 | 423-487 | 用户/管理员差异化 |

**API接口**：
```javascript
PUT /api/profile          // 更新个人信息
PUT /api/change-password  // 修改密码
```

**修改建议**：
- 添加头像上传：在第190行前添加头像区域
- 增加更多个人信息字段：修改第190-215行表单
- 修改密码强度要求：调整第575行的验证逻辑

---

### 6. carRent.html - 租车页

**功能**：浏览和租用车辆

**核心组件**：
- 筛选器：第80-120行
- 地图/列表切换：第130-150行
- 车辆卡片：第200-250行

**关键功能**：
- 地图模式：展示车辆位置
- 列表模式：展示车辆详情
- 筛选功能：按类型、价格筛选

**修改建议**：
- 添加更多筛选条件：修改第80-120行
- 修改车辆卡片样式：调整第200-250行

---

### 7. rent.html - 车辆发布页

**功能**：用户发布出租车辆

**核心元素**：
- 车辆类型选择
- 位置选择器
- 租金设置
- 提交按钮

**API接口**：
```javascript
POST /api/vehicles  // 发布车辆
```

---

### 8. admin.html - 管理员后台

**功能**：审核用户/车辆/帖子

**核心功能**：
- 待审核列表展示
- 批量审核操作
- 系统数据统计

**权限控制**：
- 页面加载时验证role === 'admin'
- 所有API请求带Authorization头

---

### 9. post.html - ❌ 废弃文件

**状态**：已废弃，建议删除

**原因**：
- 早期的简单发布页面
- 功能已被`forum.html`的发布Modal完全替代
- 项目中无任何链接指向此文件

**替代方案**：使用`forum.html`的发布功能

---

### 10. takeService.html - ⚠️ 重定向页面

**状态**：重定向页面，向后兼容

**功能**：
- 显示提示信息："代拿服务已搬家"
- 1.2秒后自动跳转到`forum.html#takeServiceSection`
- 提供手动跳转按钮

**是否需要**：
- 如果有用户收藏了旧链接，建议保留3-6个月
- 如果确认无人使用，可以删除

**代码**（第44-48行）：
```javascript
setTimeout(() => {
  window.location.replace('/forum.html#takeServiceSection');
}, 1200);
```

---

## 🎯 页面关系图

```
登录页 (login.html)
  ├─→ 首页 (index.html)
  ├─→ 论坛 (forum.html)
  ├─→ 租车 (carRent.html)
  ├─→ 个人信息 (personalInfo.html)
  │     ├─→ 车辆发布 (rent.html)
  │     └─→ 管理员后台 (admin.html) [仅管理员]
  └─→ 注册页 (register.html)
```

---

## 🔧 通用组件说明

### 导航栏组件

**位置**：每个页面顶部

**标准结构**：
```html
<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <div class="container">
    <a class="navbar-brand">校园车辆管理系统</a>
    <ul class="navbar-nav">
      <li><a href="/index.html">首页</a></li>
      <li><a href="/carRent.html">租车</a></li>
      <li><a href="/forum.html">论坛</a></li>
      <li class="dropdown">个人中心</li>
    </ul>
  </div>
</nav>
```

**修改建议**：
- 添加新菜单项：在navbar-nav中添加<li>
- 修改导航栏颜色：调整bg-white类

---

### Modal弹窗组件

**使用场景**：
- 发布帖子
- 编辑信息
- 修改密码
- 接单详情

**标准结构**：
```html
<div class="modal fade" id="myModal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">标题</div>
      <div class="modal-body">内容</div>
      <div class="modal-footer">按钮</div>
    </div>
  </div>
</div>
```

**JavaScript控制**：
```javascript
const modal = bootstrap.Modal.getOrCreateInstance(element);
modal.show();  // 显示
modal.hide();  // 隐藏
```

---

## 🎨 样式系统

### 全局样式 (assets/style.css)

**核心样式类**：
```css
.auth-container     /* 登录/注册页面居中布局 */
.auth-card          /* 认证卡片样式 */
.filter             /* 筛选器容器 */
.vehicle-card       /* 车辆卡片 */
.form               /* 表单容器 */
.pending-section    /* 待审核区域 */
```

### Bootstrap覆盖

项目使用Bootstrap 5.3.0，部分页面通过`<style>`标签覆盖默认样式。

**示例**：
```html
<style>
  .navbar-brand {
    font-size: 1.8rem !important;
    font-weight: bold;
  }
</style>
```

---

## 🔐 认证机制

### localStorage存储

所有页面使用localStorage存储用户信息：

```javascript
// 登录成功后存储
localStorage.setItem('token', token);
localStorage.setItem('userInfo', JSON.stringify(userInfo));
localStorage.setItem('user_id', user_id);

// 页面加载时验证
const token = localStorage.getItem('token');
if (!token) {
  window.location.href = '/login.html';
}
```

### API请求头

所有API请求需要带Authorization头：

```javascript
fetch('/api/endpoint', {
  headers: {
    'Authorization': 'Bearer ' + localStorage.getItem('user_id')
  }
})
```

---

## 📝 修改指南

### 1. 修改页面布局

**步骤**：
1. 打开对应HTML文件
2. 找到`<body>`标签内的主容器
3. 使用Bootstrap栅格系统调整布局
4. 测试响应式效果

**示例**：
```html
<!-- 修改前：单列布局 -->
<div class="container">
  <div class="row">
    <div class="col-12">内容</div>
  </div>
</div>

<!-- 修改后：双列布局 -->
<div class="container">
  <div class="row">
    <div class="col-md-6">左侧内容</div>
    <div class="col-md-6">右侧内容</div>
  </div>
</div>
```

### 2. 添加新功能

**步骤**：
1. 在HTML中添加UI元素
2. 在`<script>`标签中添加事件监听
3. 调用API或执行本地逻辑
4. 更新UI显示结果

**示例**（forum.html添加点赞功能）：
```javascript
// 1. HTML添加按钮
<button class="btn-like" data-post-id="${post.post_id}">👍</button>

// 2. 绑定事件
document.querySelectorAll('.btn-like').forEach(btn => {
  btn.addEventListener('click', async function() {
    const postId = this.dataset.postId;
    await fetch(`/api/posts/${postId}/like`, {method: 'POST'});
    alert('点赞成功');
  });
});
```

### 3. 修改样式

**方法1：修改全局样式**
- 编辑 `assets/style.css`
- 影响所有使用该类的元素

**方法2：添加页面内样式**
- 在`<head>`中添加`<style>`标签
- 只影响当前页面

**方法3：使用内联样式**
- 直接在元素上添加`style`属性
- 优先级最高，但不推荐

### 4. 调试技巧

**浏览器开发者工具**：
1. F12打开开发者工具
2. Console查看JavaScript错误
3. Network查看API请求
4. Elements查看DOM结构

**常见问题**：
- API返回401：检查token是否有效
- 元素点击无效：检查事件是否绑定
- 样式不生效：检查CSS选择器优先级

---

## 🚀 快速开始

### 启动项目

```bash
# 1. 启动后端
cd backend
node server.js

# 2. 访问前端
http://localhost:3000/login.html
```

### 测试账号

```
普通用户：
学号：202101
密码：123456

管理员：
账号：admin
密码：admin123
```

---

## 📞 常见问题

**Q: 如何添加新页面？**
A: 复制现有页面，修改内容，在导航栏添加链接。

**Q: Modal不显示怎么办？**
A: 检查是否正确引入Bootstrap JS，使用`getOrCreateInstance`方法。

**Q: API请求失败？**
A: 检查后端是否运行，检查Authorization头是否正确。

**Q: 样式修改不生效？**
A: 清除浏览器缓存（Ctrl+Shift+R），检查CSS选择器优先级。

---

## 📚 参考资源

- [Bootstrap 5 文档](https://getbootstrap.com/docs/5.3/)
- [高德地图API](https://lbs.amap.com/api/javascript-api/summary)
- [MDN JavaScript](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript)

---

**文档版本**：v1.0
**最后更新**：2024-11-25
**维护者**：开发团队
